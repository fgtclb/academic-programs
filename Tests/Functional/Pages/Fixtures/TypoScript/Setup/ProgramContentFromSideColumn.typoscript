# An integrator adjusting the content of program pages: the side column instead
# of the main column. Inside the same condition as the extension, so pages of
# other types are not touched.
[page && traverse(page, "doktype") == 20]
  page.10.variables.programContent.select.where = {#colPos}=1
[END]
