# Page TSconfig of the program finder content element.
#
# Delivered by the set "fgtclb/academic-programs-program-finder" and
# registered for the page field "Page TSconfig" by
# "Configuration/TCA/Overrides/pages.php" - one physical copy, two ways to it.
#
# The content element is hidden for the whole installation by
# "Configuration/page.tsconfig"; this file is what makes it selectable again,
# so it applies where the component is actually wanted instead of everywhere.
TCEFORM.tt_content.CType.removeItems := removeFromList(academicprograms_programfinder)

mod.wizards.newContentElement.wizardItems.academic {
  elements {
    academicprograms_programfinder {
      iconIdentifier = academic-programs
      title = LLL:EXT:academic_programs/Resources/Private/Language/locallang_be.xlf:plugin.program_finder.title
      description = LLL:EXT:academic_programs/Resources/Private/Language/locallang_be.xlf:plugin.program_finder.description
      tt_content_defValues {
        CType = academicprograms_programfinder
      }
    }
  }
}
